## 1. 信息架构与路由规划

- [x] 1.1 将已批准页面层级转为具体前端路由图，覆盖 `login`、`dashboard`、`users`、`users/:id`、`clients`、`clients/:id`、`proxies`、`proxies/:id`、`certificates` 和 `audit`
- [x] 1.2 定义认证 shell 职责，包括导航 chrome、当前会话展示、登出入口和路由级错误处理
- [x] 1.3 决定实现中的根路由行为，使用 `/` 按会话状态导向 `/dashboard` 或 `/login`

## 2. 会话守卫与 Shell 启动规划

- [x] 2.1 围绕 `/api/admin/session` 定义硬加载、深链和登录后初始化的前端启动序列
- [x] 2.2 使用同源会话端点定义缺失会话、过期会话和显式登出流程的受保护路由行为
- [x] 2.3 定义成功登录后的目标恢复方式，并确保会话校验前不暴露受保护页面内容

## 3. 页面容器与数据消费规划

- [x] 3.1 将每个已确认页面映射到规范后端合同，区分会话端点交互与 GraphQL 列表、详情和变更流程
- [x] 3.2 定义 `users`、`clients` 和 `proxies` 的列表/详情容器边界，以及路由参数如何驱动规范详情查询
- [x] 3.3 定义 `dashboard`、`certificates` 和 `audit` 的顶层页面容器预期，不假设未支持的详情路由

## 4. 共享 UX 状态规划

- [x] 4.1 定义可复用页面状态模式：加载、基线空状态、过滤空状态、未找到、校验失败、后端失败和会话过期
- [x] 4.2 定义列表和详情页的变更后刷新预期，使实现可以重新查询规范 GraphQL 数据而无需重载整个 shell
- [x] 4.3 定义代理和证书操作等破坏性或生命周期动作的确认预期

## 5. 轮询与验收规划

- [x] 5.1 按已批准前端模型定义 dashboard、clients、proxies、certificates、users 和 audit 的页面级轮询归属和节奏
- [x] 5.2 定义路由守卫、shell 导航、深链启动和页面状态处理相对于同源 admin API 基线的验收场景
- [x] 5.3 定义规范 GraphQL 消费验收场景，使后续前端实现可验证列表/详情接线而不重定义后端语义
