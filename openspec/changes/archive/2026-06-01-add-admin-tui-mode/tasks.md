## 1. 命令入口与依赖

- [x] 1.1 在 `cmd/goginx-admin` 的 usage 和命令分发中新增 `tui` 子命令，支持 `-db`、`-actor` 并保持现有子命令行为不变。
- [x] 1.2 引入选定的 Go TUI 依赖，并确认依赖只被 admin 命令路径引用。
- [x] 1.3 新增 `internal/admintui` 包骨架，定义运行选项、服务接口、页面状态和启动函数。
- [x] 1.4 在进入 TUI 前检测交互式终端能力；不支持时返回明确错误并提示使用非交互式子命令。

## 2. 本地服务适配

- [x] 2.1 抽取或复用 `goginx-admin` 的本地 SQLite 打开逻辑，让 TUI 使用相同默认数据库路径和部署相对路径解析。
- [x] 2.2 为 TUI 实现 admin command/query 适配器，封装管理员、用户、客户端、join token 和维护动作的读写操作。
- [x] 2.3 为用户删除补充受保护的服务层语义，避免通过 SQLite 级联误删客户端或代理。
- [x] 2.4 将服务层结构化校验、冲突和不存在错误转换为 TUI 可展示的字段级或流程级错误。
- [x] 2.5 为一次性客户端凭据和 join token 结果定义只在当前结果页展示的数据模型。

## 3. TUI 导航与通用交互

- [x] 3.1 实现主菜单，包含管理员设置、用户管理、客户端配置和退出。
- [x] 3.2 实现通用列表选择、文本输入、密码输入、确认页、结果页和错误展示组件或模型。
- [x] 3.3 实现取消、返回上级、完成后回主菜单和退出行为，确保取消确认前不写入资源。
- [x] 3.4 为窗口尺寸不足、空列表和加载/提交中状态提供可理解的终端提示。

## 4. 管理员设置流程

- [x] 4.1 查询并展示现有管理员列表，包含用户名、用户 ID、角色和状态。
- [x] 4.2 实现首次管理员创建流程，校验用户名、密码和确认密码，并调用现有管理员初始化行为。
- [x] 4.3 实现选择现有管理员后更新密码流程，校验密码和确认密码并保持管理员可登录。
- [x] 4.4 实现禁用管理员的启用确认流程，并复用服务层审计。
- [x] 4.5 覆盖管理员流程的字段错误、服务错误、取消和成功结果状态。

## 5. 用户配置流程

- [x] 5.1 查询并展示现有用户列表，包含用户名、用户 ID、角色和状态。
- [x] 5.2 实现创建用户表单，角色使用 `user`/`admin` 选项，默认选择 `user`。
- [x] 5.3 在提交前校验用户名和角色选项，并在服务层冲突或校验失败时保留已填字段。
- [x] 5.4 创建成功后展示新用户摘要，并允许继续创建用户或返回主菜单。
- [x] 5.5 实现用户启用、禁用和受保护删除流程，删除前要求强确认并阻止带依赖资源的删除。

## 6. 客户端配置流程

- [x] 6.1 查询用户列表并作为客户端所属者选择器；无用户时阻止创建客户端并引导先创建用户。
- [x] 6.2 实现默认客户端快速向导，按用户选择、客户端名称和 join 参数完成客户端与令牌创建。
- [x] 6.3 实现仅创建客户端凭据的次级路径，默认使用服务层生成凭据，手动凭据作为显式选项。
- [x] 6.4 在提交前校验客户端名称、用户选择、join 必填地址和 CA 文件存在性。
- [x] 6.5 成功后只在当前结果页展示客户端凭据或 join token，并给出一次性展示提示。
- [x] 6.6 实现客户端启用、禁用、凭据轮换和受保护删除流程，删除前要求强确认并尊重服务层阻塞条件。

## 7. 测试与文档

- [x] 7.1 为 `cmd/goginx-admin` 增加 `tui` 子命令 flag、默认数据库路径和非交互式终端拒绝行为测试。
- [x] 7.2 为 `internal/admintui` 增加模型测试，覆盖主菜单导航、取消、确认页和错误状态。
- [x] 7.3 为管理员、用户和客户端流程增加测试，覆盖成功路径、强校验、维护动作、服务错误映射和一次性 secret/token 展示状态。
- [x] 7.4 更新 README 或运维文档，说明 `goginx-admin tui` 的定位、默认路径、首版范围和与脚本式子命令的关系。
- [x] 7.5 运行 `go test ./cmd/goginx-admin ./internal/admin ./internal/adminquery` 和 OpenSpec 校验，确认变更可实现且规格通过。
