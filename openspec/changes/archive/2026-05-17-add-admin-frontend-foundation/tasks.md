## 1. 前端工作区与交付基础

- [x] 1.1 选择并搭建 admin 前端工作区和框架，使浏览器路由可在 admin origin 下服务且不冲突 `/api/admin/*`
- [x] 1.2 定义并实现本地开发同源交付接线，使前端浏览器流量可以访问 admin API，同时不引入生产中不存在的跨源行为
- [x] 1.3 定义并实现面向生产的前端资源交付和路由回退行为，覆盖 `/`、`/login`、`/dashboard`、`/users`、`/clients`、`/proxies`、`/certificates` 和 `/audit`，并保留 `/api/admin/*` 作为 API 命名空间

## 2. Shell 与会话启动

- [x] 2.1 实现公开与受保护路由组，使 `/login` 成为唯一公开路由，并基于 `GET /api/admin/session` 处理 `/` 重定向
- [x] 2.2 实现受保护 shell 启动，使硬加载、刷新和深链在渲染受保护内容前校验当前管理员会话
- [x] 2.3 实现成功登录后的目标恢复，并在缺失或无效目标时安全回退到 `/dashboard`
- [x] 2.4 实现显式登出行为，清理 shell 会话状态并返回 `/login`

## 3. 请求层与错误语义

- [x] 3.1 为 `/api/admin/login`、`/api/admin/session` 和 `/api/admin/logout` 实现会话导向请求客户端
- [x] 3.2 为 `/api/admin/graphql` 实现 GraphQL 请求客户端，包含规范响应解析和共享 transport 行为
- [x] 3.3 实现 CSRF 感知变更处理，使浏览器写操作在需要时携带会话启动令牌
- [x] 3.4 实现集中式前端错误映射，覆盖 `UNAUTHENTICATED`、`FORBIDDEN`、`VALIDATION_FAILED`、`NOT_FOUND`、`CONFLICT`、`UNSUPPORTED`、`ENTRY_CONFLICT` 和 `INTERNAL`

## 4. 共享 Shell 与页面状态原语

- [x] 4.1 实现认证 shell 原语，包括导航 chrome、当前管理员展示、路由级加载和认证过期处理
- [x] 4.2 实现共享页面状态原语，包括加载、基线空状态、过滤空状态、未找到、校验失败和后端失败
- [x] 4.3 实现破坏性或生命周期动作的共享确认模式，例如代理禁用/删除和证书签发/续期

## 5. 页面容器实现

- [x] 5.1 实现 dashboard 页面容器，包含摘要读取和页面级 5 秒轮询
- [x] 5.2 实现 users 列表和详情页面容器，包含创建、禁用、密码修改流程和变更后定向刷新
- [x] 5.3 实现 clients 列表和详情页面容器，包含运行时感知读取和页面级轮询
- [x] 5.4 实现 proxies 列表和详情页面容器，包含创建、更新、启用、禁用、禁用后删除、显式 `ENTRY_CONFLICT` 处理和页面级轮询
- [x] 5.5 实现 certificates 页面容器，包含生命周期状态读取、签发和续期动作，以及低频或动作驱动刷新
- [x] 5.6 实现 audit 页面容器，包含近期事件读取、轻量过滤和手动或低频刷新

## 6. 验证

- [x] 6.1 增加前端覆盖，验证根路由重定向、登录重定向、受保护路由启动和目标恢复
- [x] 6.2 增加共享页面状态覆盖，包括认证过期、未找到、校验失败、基线空状态和过滤空状态的实现路径
- [x] 6.3 增加页面级轮询归属和变更后定向刷新行为的实现验证路径
- [x] 6.4 增加同源前端与 `/api/admin/*` 在本地开发和生产式服务路径中的集成覆盖
