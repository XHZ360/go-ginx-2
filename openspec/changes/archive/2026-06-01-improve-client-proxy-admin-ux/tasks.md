## 1. 服务地址确认与 join 默认值

- [x] 1.1 梳理现有 server 配置、默认监听和 join token 生成路径，确定确认服务地址字段的落点
- [x] 1.2 实现显式服务域名/IP 覆盖、configless 自动推断和格式校验
- [x] 1.3 在服务端启动日志或管理启动信息中暴露默认服务地址及来源
- [x] 1.4 更新 create-client-join CLI、admin GraphQL/API 和服务层，使未显式传入地址时使用确认服务地址
- [x] 1.5 补充服务地址显式覆盖、自动推断、无效配置失败和 join token 默认地址测试

## 2. 客户端完全删除

- [x] 2.1 增加客户端永久删除 command/store 能力，并在事务内清理客户端凭据、join/enrollment 残留和允许级联的从属资源
- [x] 2.2 在删除前检测关联启用代理，返回结构化冲突语义并保留现有资源
- [x] 2.3 暴露 admin GraphQL `deleteClient` mutation 和前端可消费 payload/error 语义
- [x] 2.4 为删除成功、启用代理冲突、重复删除或不存在客户端补充服务层、GraphQL 和存储测试
- [x] 2.5 确认删除后的客户端无法再认证、出现在列表/详情或消费 join/enrollment 材料

## 3. 客户端到代理创建流程

- [x] 3.1 在 Clients 列表和 ClientDetail 页面增加“为此客户端创建代理”入口
- [x] 3.2 为代理创建路由或 URL 参数携带 `userId` 与 `clientId` 默认上下文，并支持刷新和登录后恢复
- [x] 3.3 更新代理创建表单，增加用户选择器和客户端选择器的默认值、ID fallback 与可编辑行为
- [x] 3.4 在用户选择变化时刷新或过滤客户端候选项，并阻止提交不一致的用户/客户端组合
- [x] 3.5 补充前端测试，覆盖客户端入口跳转、默认选择器、修改选择器和候选项未加载时的 fallback

## 4. 弹窗与 token 展示交互

- [x] 4.1 统一 Dialog/Modal 基础组件最大高度和内部滚动布局，确保标题、主体和操作区在小视口可访问
- [x] 4.2 更新 join token、客户端凭据和 secret 展示组件，使长文本区域最多显示 3 行并内部滚动
- [x] 4.3 调整蒙版关闭逻辑，仅在鼠标按下和释放都发生在蒙版层时关闭弹窗
- [x] 4.4 补充弹窗组件和关键页面测试，覆盖内容溢出、长 token、完整蒙版点击关闭和从弹窗按下后在蒙版释放不关闭

## 5. 集成验证与文档

- [x] 5.1 更新用户可见文档或操作说明，描述服务地址默认值、覆盖方式、客户端删除限制和从客户端创建代理入口
- [x] 5.2 运行 Go 单元测试、admin-ui 测试和相关端到端或构建验证
- [x] 5.3 手动验证 admin-ui 关键路径：生成 join token、删除客户端、从客户端创建代理、弹窗滚动和蒙版关闭语义
